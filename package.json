{
  "name": "monad-pevm-memory-allocator",
  "version": "1.0.0",
  "description": "Thread-isolated memory arena management framework for parallel EVM worker layers",
  "main": "testArenaAllocation.js",
  "dependencies": {
    "dotenv": "^16.4.5"
  }
}
